{
  "name": "@aura-note/ai-gateway",
  "private": true,
  "version": "0.1.0",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "lint:phi": "node ../../scripts/lint-phi.js packages/ai-gateway",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --import tsx --test src/index.test.ts",
    "test:unit": "pnpm test",
    "test:e2e": "node --import tsx --test src/index.test.ts"
  },
  "devDependencies": { "typescript": "^5.6.0" }
}
