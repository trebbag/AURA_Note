{
  "name": "@aura-note/ai-gateway",
  "private": true,
  "version": "0.1.0",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo ai-gateway lint pending",
    "lint:phi": "node ../../scripts/lint-phi.js packages/ai-gateway",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "echo ai-gateway tests pending",
    "test:unit": "echo ai-gateway unit tests pending",
    "test:e2e": "echo ai-gateway e2e tests pending"
  },
  "devDependencies": { "typescript": "^5.6.0" }
}
