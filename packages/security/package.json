{
  "name": "@aura-note/security",
  "private": true,
  "version": "0.1.0",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo security lint pending",
    "lint:phi": "echo security package owns PHI detection policy; skipping string scan for this package",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "echo security tests pending",
    "test:unit": "echo security unit tests pending",
    "test:e2e": "echo security e2e tests pending"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
