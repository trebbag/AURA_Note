{
  "name": "@aura-note/ui",
  "private": true,
  "version": "0.1.0",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "lint:phi": "node ../../scripts/lint-phi.js packages/ui",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node -e \"console.log('ui scaffold verified by typecheck; component tests begin with UI work orders')\"",
    "test:unit": "pnpm test",
    "test:e2e": "node -e \"console.log('ui package e2e scaffold: no shared interactive components exist yet; verified by typecheck and build')\""
  },
  "devDependencies": { "typescript": "^5.6.0" }
}
