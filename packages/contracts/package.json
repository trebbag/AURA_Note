{
  "name": "@aura-note/contracts",
  "private": true,
  "version": "0.1.0",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "lint:phi": "node ../../scripts/lint-phi.js packages/contracts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --import tsx --test src/index.test.ts",
    "test:unit": "pnpm test",
    "test:e2e": "node -e \"console.log('contracts e2e deferred until API flows are implemented')\""
  },
  "dependencies": {
    "@aura-note/domain": "workspace:*"
  },
  "devDependencies": { "typescript": "^5.6.0" }
}
