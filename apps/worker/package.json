{
  "name": "@aura-note/worker",
  "private": true,
  "scripts": {
    "dev": "tsx src/main.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "echo worker lint pending",
    "lint:phi": "node ../../scripts/lint-phi.js apps/worker",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "echo worker tests pending",
    "test:unit": "echo worker unit tests pending",
    "test:e2e": "echo worker e2e tests pending"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
